<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="onlineshopping.buyon.com.buyon">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FashionActivity" />
        <activity
            android:name=".Food"
            android:label="BuyOn - Food" />
        <activity
            android:name=".WebActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".Travel"
            android:label="BuyOn - Travel" />
        <activity
            android:name=".Recharge"
            android:label="BuyOn - Recharge" />
        <activity
            android:name=".Books"
            android:label="BuyOn - Books" />
        <activity
            android:name=".Grocery"
            android:label="BuyOn - Grocery" />
        <activity android:name=".Health" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".Offer"></activity>
    </application>

</manifest>